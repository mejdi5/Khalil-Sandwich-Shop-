const db = require('../db')

const getIngredients = async (req, res) => {
    try {
        
    } catch (error) {
        console.log(error)
        res.status(500).json(error);
    }
}

const addIngredient= async (req, res) => {
    try {
        
    } catch (error) {
        console.log(error)
    }
}

const editIngredient= async (req, res) => {
    try {
        
    } catch (error) {
        console.log(error)
    }
}

const deleteIngredient= async (req, res) => {
    try {
        
    } catch (error) {
        console.log(error)
    }
}


module.exports = { getIngredients, addIngredient, editIngredient, deleteIngredient }